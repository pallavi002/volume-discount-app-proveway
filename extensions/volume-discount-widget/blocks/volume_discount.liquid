{% assign rules = shop.metafields.volume_discount.rules.value %}
{% if rules and rules.products and rules.percentOff %}
  {% if rules.products contains product.gid %}
    <div class="volume-discount-widget" style="
      background: #f0f9ff;
      border: 2px solid #0ea5e9;
      border-radius: 8px;
      padding: 12px 16px;
      margin: 16px 0;
      font-weight: 500;
      color: #0c4a6e;
      text-align: center;
    ">
      🎉 Buy {{ rules.minQty | default: 2 }}, get {{ rules.percentOff }}% off!
    </div>
  {% endif %}
{% endif %}

{% schema %}
{
  "name": "Volume Discount Widget",
  "target": "section",
  "settings": []
}
{% endschema %}