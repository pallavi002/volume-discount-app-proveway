query CartLinesDiscountsGenerateRunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          product {
            id
          }
        }
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  shop {
    metafield(namespace: "volume_discount", key: "rules") {
      value
    }
  }
}
